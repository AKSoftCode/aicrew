---
description: "Reinforce default terse output (already on by default)"
argument-hint: ""
deprecated: true
deprecated-reason: "Use /lean on instead. /terse is an alias for /lean on and will not be updated separately."
---

> **Deprecated.** `/terse` is an alias for `/lean on`. Use `/lean on` going forward.
> See `~/Agents/commands/lean.md` for the canonical verbosity toggle.

# /terse (deprecated — use `/lean on`)

Default output is already terse/caveman. See `~/Agents/agents/caveman.md`.

Use `/lean on` to explicitly re-enable terse mode if `/normal` or `/lean off` was used earlier in the session. `/terse` is kept as a backward-compatible alias.
