interface:
  display_name: "AICrew Terse"
  short_description: "Re-enable caveman/lean terse output mode"
  brand_color: "#0F766E"
  default_prompt: "Use $aicrew-terse to re-enable lean/caveman output mode."

policy:
  allow_implicit_invocation: true
