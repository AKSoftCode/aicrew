'use strict';

// Programmatic API (used when aicrew is required as a library)
const installer = require('./installer');
const project   = require('./project');

module.exports = { installer, project };
